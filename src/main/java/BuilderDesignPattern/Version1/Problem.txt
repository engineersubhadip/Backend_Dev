In this version,
In the run-time during object creation, we are getting error.
Still ISSUE Exists.
Because at the run-time we are performing the validation checks.

While creating Student object, we are seeing that Builder object needs to be created first.
i.e. Discovery in not easy.